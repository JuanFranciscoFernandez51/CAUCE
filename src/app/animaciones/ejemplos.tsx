"use client";

import { CatalogoAnimaciones } from "@/components/animaciones/catalogo";

/** Ejemplos vivos para la web pública: solo la demo y el argumento (sin código). */
export function EjemplosAnimaciones() {
  return <CatalogoAnimaciones modo="publico" />;
}
