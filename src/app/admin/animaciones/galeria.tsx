"use client";

import { CatalogoAnimaciones } from "@/components/animaciones/catalogo";

/** Galería interna: catálogo con vista previa viva + copiar el uso para meterla en un proyecto. */
export function GaleriaAnimaciones() {
  return <CatalogoAnimaciones modo="admin" />;
}
